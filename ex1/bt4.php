<?php
/* Trong một file chỉ php mà không có html t có thể khai báo theo kiểu như sau:
    1. <?php $demo = "Hello"; ?> với kiểu khai báo này khi kết thúc câu lệnh dấu ( ; ) có cũng hoặc không có cũng được
    2. <?php $demo = "Hello"; với kiểu khai báo không có đuôi ( ?> ) thì khi kết thúc câu lệnh bắt buộc phải có dấu ( ; )
 */
$message = "PHP Comments"; // dấu ( $ ) trong PHP dùng để khai báo biến

echo $message; # echo là câu lệnh dùng để in ra giá trị muốn hiển thị 
